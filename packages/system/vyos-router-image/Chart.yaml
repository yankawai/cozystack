apiVersion: v2
name: cozy-vyos-router-image
description: Build-only package that produces the VyOS 1.5-rolling site-router appliance qcow2 and publishes it as a digest-pinned containerDisk (consumed by packages/apps/site-router). Not deployed as a HelmRelease.
version: 0.0.0 # Placeholder, the actual version will be automatically set during the build process

apiVersion: v2
name: site-router
description: Managed site-to-site router
icon: /logos/site-router.svg
type: application
version: 0.0.0 # Placeholder, the actual version will be automatically set during the build process
appVersion: "1.5-rolling"
dependencies:
  - name: cozy-lib
    version: "*"
    repository: "file://charts/cozy-lib"
